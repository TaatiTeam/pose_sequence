from setuptools import setup

setup(
        name='pose_sequence',
        version='0.0',
        packages=[
            'pose_sequence',
            'pose_sequence.visualization',
            'pose_sequence.utils'
        ],
)
